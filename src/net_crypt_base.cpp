#include "base.h"
namespace unet
{
    Crypt_base::Crypt_base() noexcept
    {
    }
    Crypt_base::~Crypt_base()
    {
    }
    int Crypt_base::send(const std::string &data, size_t len) const noexcept
    {
    }
    std::string Crypt_base::recv(int32_t timeout) const noexcept
    {
    }
    int Crypt_base::close() noexcept
    {
    }
}
